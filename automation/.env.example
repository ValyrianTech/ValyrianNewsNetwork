# VNN Push Notification Configuration
VNN_PUSH_API_KEY=your-api-key-here
VNN_PUSH_WORKER_URL=https://vnn-push.valyriannewsnetwork.workers.dev

# VNN Publish API Server
# Bearer token clients must present in the Authorization header.
VNN_PUBLISH_API_KEY=generate-a-long-random-string
# Optional overrides (defaults shown):
# VNN_API_HOST=0.0.0.0
# VNN_API_PORT=8787
